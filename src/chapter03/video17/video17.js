console.log("Video 17");

var name = "LamVoi";

const name1 = "NTLam";

let name2 = "Lam15me";

// dùng const khi giá trị không cần thay đổi (hằng số) => chỉ gán giá trị 1 lần
// phân biệt var/let: biến let giúp hạn chế khả năng sử dụng biến số (vd: chỉ được sử dụng trong 1 hàm), biến var không bị hạn chế phạm vi nhưng sẽ tiềm ẩn rủi ro
// =>  ưu tiên dùng let và const thay vì var


console.log(name)